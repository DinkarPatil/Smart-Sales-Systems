import { RegisterAdminForm } from '@/components/auth/register-admin-form';

export const metadata = { title: 'Register as admin · Smart Sales' };

export default function RegisterAdminPage() {
  return <RegisterAdminForm />;
}
